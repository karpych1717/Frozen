class Button {
  constructor (x, y, w, h, callback) {
    this.x = x
    this.y = y
    this.w = w
    this.h = h

    this.onclick = callback
  }

  drawIt (context) {

  }

  isOverIt (x, y) {
    
  }
}

export default Button
